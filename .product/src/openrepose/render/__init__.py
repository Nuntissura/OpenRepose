"""Rendering subpackage for OpenRepose.

Modules:
    draw_3d           pure-numpy 3D wireframe render of a Rig (no pyrender dep)
    draw_openpose     native OpenPose-format wireframe render of a RotatedRig
    widget_grab       QWidget.grab() wrapper with placeholder fallback for headless
    compose           composite child snapshots into the full_window image
"""
